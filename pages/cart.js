import Layout from '../src/components/layout/Layout';
import CartPageContainer from '../src/container/cart/CartPageContainer';

const Cart = () => {
  return (
    <Layout>
      <CartPageContainer />
    </Layout>
  );
};

export default Cart;
