import LoginContainer from '../src/container/auth/LoginContainer';
import Layout from '../src/components/layout/Layout';

const Login = () => (
  <Layout>
    <LoginContainer />
  </Layout>
);

export default Login;
